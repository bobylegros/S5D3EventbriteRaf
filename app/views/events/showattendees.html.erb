<h1>Liste des participants à l'Event</h1>
<h2>event.name</h2>

<table class="table table-hover">
  <tr>
    <th>Nom</th>
    <th>email</th>
  </tr>

  <%@event.attendees.each do |attendee|%>
    <tr>
      <td><%= attendee.name %></td>
      <td><%= attendee.email %></td>
    </tr>
  <%end%>
</table>
